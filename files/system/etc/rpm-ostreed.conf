[Daemon]
# uupd is the single source of truth for OS-image updates on this image
# (see uupd.timer). Leaving rpm-ostreed's own policy at its uBlue-inherited
# default of "stage" would mean two things trying to stage/apply upgrades
# on their own schedules. "none" leaves upgrade staging entirely to uupd.
AutomaticUpdatePolicy=none
